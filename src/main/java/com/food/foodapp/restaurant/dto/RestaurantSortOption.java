package com.food.foodapp.restaurant.dto;

/** Supported values for the restaurants list `sort` query parameter. */
public enum RestaurantSortOption {
    RATING,
    DELIVERY_TIME,
    DELIVERY_FEE
}
